// Transpiled from GLSL to Lux
// Original GLSL version: 450

fragment {
    in fragNormal: vec3;
    in fragPos: vec3;
    out outColor: vec4;
    
    fn main() {
        let n: vec3 = normalize(fragNormal);
        let l: vec3 = normalize(lightDir);
        let ndotl: scalar = max(dot(n, l), 0.0);
        let diffuse: vec3 = (albedo * ndotl);
        outColor = vec4(diffuse, 1.0);
    }
}